import { ActivityIndicator as ReactActivityIndicator } from 'react-native';

const ActivityIndicator = () => <ReactActivityIndicator size="large" color="#4CAF50" />;

ActivityIndicator.displayName = 'Text';

export { ActivityIndicator };
